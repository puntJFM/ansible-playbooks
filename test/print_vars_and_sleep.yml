---
- name: Print all extra vars and sleep
  hosts: localhost
  gather_facts: no
  
  tasks:
    - name: FULL DUMP (no filtering)
      debug:
        var: vars

    - name: Sleep 15
      command: sleep 15

